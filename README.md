# Metabase Backend

Spring Boot backend for a connected media catalogue.

## Development

```sh
docker compose up -d
./gradlew test
```

GraphiQL is available at `http://localhost:8080/graphiql` while the application is running.

